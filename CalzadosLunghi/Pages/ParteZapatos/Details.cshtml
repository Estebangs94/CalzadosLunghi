@page "id:int"
@model CalzadosLunghi.Pages.ParteZapatos.DetailsModel

@{
    ViewData["Title"] = "Details";
}

@if (Model.Message != null)
{
    <div class="alert alert-success">@Model.Message</div>
}

<h1>Detalles</h1>

<div>
    <h4>Componente de zapato</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ParteZapato.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ParteZapato.Nombre)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ParteZapato.FormadoPorMultipleMateriales)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ParteZapato.FormadoPorMultipleMaterialesString)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ParteZapato.ID">Editar</a> |
    <a asp-page="./Index">Volver a la lista</a>
</div>
