@page
@model CalzadosLunghi.Pages.ParteZapatos.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ParteZapato</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ParteZapato.ID" />
            <div class="form-group">
                <label asp-for="ParteZapato.Nombre" class="control-label"></label>
                <input asp-for="ParteZapato.Nombre" class="form-control" />
                <span asp-validation-for="ParteZapato.Nombre" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="ParteZapato.FormadoPorMultipleMateriales" /> @Html.DisplayNameFor(model => model.ParteZapato.FormadoPorMultipleMateriales)
                </label>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="ParteZapato.EstaActivo" /> @Html.DisplayNameFor(model => model.ParteZapato.EstaActivo)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
